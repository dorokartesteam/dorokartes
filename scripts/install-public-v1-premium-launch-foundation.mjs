import fs from "node:fs";import path from "node:path";const root=process.cwd(),payload=path.join(root,"payload");if(!fs.existsSync(payload)){console.error("Missing payload folder. Extract ZIP into project root.");process.exit(1)}function copy(src,dst){fs.mkdirSync(dst,{recursive:true});for(const e of fs.readdirSync(src,{withFileTypes:true})){const s=path.join(src,e.name),d=path.join(dst,e.name);e.isDirectory()?copy(s,d):fs.copyFileSync(s,d)}}copy(payload,root);fs.rmSync(payload,{recursive:true,force:true});console.log("Dorokartes Public v1 installed. No schema/data changes.")
